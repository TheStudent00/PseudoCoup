# On-deck — next sub-tasks

Edit this list freely; `track.py render` folds it into PROGRESS.html / .md. Format: `- [area] task — why/where`.
Top of the list is what's next.

The app RUNS in Python (boots, navigates by real buttons, real db) and the LAYOUT-FIDELITY instrument is
live: the real Compose engine (Robolectric, headless) and the kivy kit both dump every component's box;
`layout_diff.py` compares them as %-of-display within a tolerance band. That number is now a measured
gauge on this board — the continue/shutdown instrument.

- [fidelity] 305/322 components within tolerance, 25 of 28 screens at 100% (the original 20 all hold). This round's five general fixes: (1) differ pairing order — repeated texts' below-fold zero-bound compose copies stole visible kivy twins (ProgramEditor's 13-week roadmap); visible-first pairing on BOTH sides + "invisible in EITHER engine" now covers off-right (horizontal scroll) and zero-area, not just below-fold. (2) buildAnnotatedString was an autostub — now a transpiler builder scope (like buildString) + real AnnotatedStringBuilder; the MACROCYCLE headers render. (3) The dump's fixed 2.6s settle read geometry mid-flight under xvfb (labels thousands of px from correct parents — this WAS the "unmounted twin" 100x100 mystery, timing not a phantom tree); replaced with settle detection (two identical consecutive geometry snapshots). (4) ListItem was stacked as a column (trailing IconButton under the text: +8px per row, no 16dp inset); now a real M3 row (leading | overline/headline/supporting | trailing, 56/72/88 min heights). (5) fidelity.py gradle now --max-workers=1 -Xmx2g (RAM crash guard). Results: ProgramEditor 0/34→31/32, ExercisePicker 6/46→26/26.
- [fidelity] 373/377 (98.9%), 26/28 screens at 100%. Second wave this round (3 general fixes + 2 harness fixes, the last three delegated to Opus subagents under tight briefs): (1) ListItem M3 row + Box fill-width propagation in _zstack (exec table columns spread; 8/31→28/31). (2) Compose-suite hermeticity: per-test Room executors in the generated assembler — the leak was contention on the JVM-static ArchTaskExecutor pool delaying ProgramEditor's first flow emission past the dump (3-test conjunction, bisected). (3) Session fixtures pin startedAt/completedAt to the fixed instant on BOTH engines (SessionDetail 14/14). (4) Transpiler wave from exec-screen diagnosis, all general: forEach/forEachIndexed with bare non-local return lower to real for loops; `!f(a,b)` unary-on-callee parse quirk; extension PROPERTIES patch as @property with sibling+collection-API receiver members; suspend relation DAO queries return values not Flows; imports count as known free names (builder-scope misbinding); lambda scopes carry body locals (nonlocal emission); KtList.mapIndexedNotNull; Room relation rows are KtList.
- [fidelity] MILESTONE: 377/377 — 100% of every component on ALL 28 measurable screens. Closers: (1) 12sp SemiBold shaper band specimen-derived (SemiBold widening is size-dependent: 1.006 at <=12sp vs 1.042 at 24sp) — ProgramEditor 32/32. (2) TextFieldValue was an inert stub destroying field values at construction — now a real class + recorder unwrap; Modifier.size(width=,height=) keyword form honored — WorkoutExecution 31/31. (3) ProgramEditor compose dump anchored on the read-only branch text (the stateIn initial value renders the editable branch until Room emits; executor isolation alone didn't close the race).
- [fidelity] COVERAGE COMPLETE (verified exhaustive): the app has 29 top-level Screen composables; 28 measured at 100%, DebugPanel is the sole intentional skip (dev-only; SAF launchers need a real Activity). The geometry gauge has nothing left to measure — next fidelity work is behavioral/visual, not structural.
- [fidelity] Sanctioned non-general bridges (user-approved as engine/font-specific, all specimen-pinned): shaper width calibration by size/weight band, and emoji advance = 1.28 x fontSize. Everything else remains general.
- [fidelity] SPECIMEN gate is live in fidelity.py (synthetic, not counted, fail-loud): the text-metric rules (natural single-line stacking; letterSpacing widths) are DERIVED from dumpSpecimen. Extend it when a new metric question appears — never infer from mixed app screens.
- [ui] Paint layer STARTED (two delegated slices): (1) kit paint mechanism — canvas.before fill/border from recorded background/clip/border ops + M3 surface roles + text color, all resolvers stub-guarded (unresolved = no paint, never invented). (2) Runtime color table real: Color (packed Int32 masked to unsigned), RoundedCornerShape/CircleShape radii, light/darkColorScheme, MaterialTheme.colorScheme resolving through the app's installed theme; Color.Unspecified stays an unresolvable sentinel. Geometry gate held 377/377 both times. Shots in layout_inspect/shots/. Pass 2 done: top-bar surface role, M3 button/chip role map, selected-chip rule; text contrast was a stale-shot artifact (already live). Pass 3: icons DONE — real MaterialIcons TTF + shipped codepoints from the OS package (36/36 app icon names; unmapped stays empty, never a wrong glyph). Pass 4 (fields/chips) DONE: OutlinedTextField 1dp outline-role stroke + surface-backed label notch (unfocused spec state only — no faked focus); filled TextField surfaceVariant container + bottom indicator; chips confirmed correct from pass 2. Shots: ReportBug/ExerciseCreate/ExercisePicker before+after. Remaining paint: ELEVATION as one general mechanism decision (top-bar tint + chip/card shadow together — user call), then done. NOTE for next session: system icon font is an OS package dependency (fonts-material-design-icons-iconfont) — consider vendoring into render/assets with user approval for portability.
- [ui] Popups DONE: excluded from layout (closed) + real overlays in the running app (open): presence=open for dialogs/sheets, expanded-flag for menus, tap-outside dismisses through the recorded handler; measurement path isolated (OVERLAYS_ENABLED, run_app-only). Proof shot: layout_inspect/shots/popup_proof.png.
- [ui] ELEVATION DONE (user-approved, one general mechanism): _ELEVATION_LEVEL role table (Card/ElevatedChip L1, TopAppBar L2 via M3 scrolledContainer=surfaceColorAtElevation(3dp), FAB L3); _tonal_fill overlays surfaceTint(primary) on surface at the M3 alpha table (5/8/11/12/14% L1-5); _draw_shadow = stacked translucent rounded rects. Colors from theme, alphas from spec — nothing invented. Geometry held 377/377. Report: DevComms/elevation_mechanism.md.
- [ui] REAL-APP VISUAL SWEEP + FIXES: full eyeball of all 27 screens through run_app (DevComms/realapp_visual_sweep.md) — 0 crash, 0 broken, 19 OK, 8 minor. All 8 closed as GENERAL fixes (two delegated slices, disjoint files, gates held 377/377 + 513/513): (1) Tab composables were in BUTTON_KINDS with no theme branch (kept Kivy's gray default; the "light" screens use FilterChip) — added TAB_KINDS + _theme_button branch (transparent container, primary/onSurfaceVariant labels): Exercises/Progress tabs now themed. (2) "Tofu" rows were NOT missing Material icons — they're bare Text(emoji) graphemes in the app font (Figtree, no emoji); NotoColorEmoji segfaults SDL text, so a PIL raster path (_emoji_pil_font/_emoji_texture/_draw_emoji, canvas.after) draws real color emoji: Warmup/Cooldown/wizards now show glyphs. NEW DEP: Pillow + NotoColorEmoji font (real-glyph bridge, no invention; vendoring candidate like the icon TTF). (3) Switch/Checkbox/Radio reserved a box but painted nothing — kind-driven _draw_control from theme roles: ExerciseCreate toggles now visible. (4) PathDetail direct-launch showed "Path not found." — its pathId is a direct fn param (no VM/SavedStateHandle), so SCREEN_ARGS never reached it; _bind_direct_nav_args pre-fills it, GATED on OVERLAYS_ENABLED so the DUMP still renders the empty ground-truth state (gauge stayed 377/377, PathDetail 1/1). Reports: paint_gaps_fix.md, launcher_fixtures.md. Real-app shots verified: verify_Exercises/Warmup/ExerciseCreate, fix_pathdetail.
- [ui] LAUNCHER FIDELITY (two real bugs the geometry gauge structurally can't see, found while wiring the user's run command): (1) run_app composed the BARE screen — the dump path wraps every screen in WorkoutforlifeTheme (installs Typography + colorScheme); without it every M3 color role resolved to the inert default and the real app rendered GREYSCALE. run_app now wraps identically (darkTheme=False, matches ground truth). (2) A bare-Column screen (Settings — no Scaffold) is transparent in Compose and shows the theme window background; Kivy's window defaults to BLACK, so dark-on-light text was invisible. run_app now paints Window.clearcolor = colorScheme.background (one fix, every non-Scaffold screen). Real-app shots now faithful M3 light: themed_gymlist2 (lavender elevated top bar + card + FAB), themed_settings2 (readable, purple section headers, outlined fields, selected segment). This is why fidelity=377/377 while the launched app looked wrong — the instrument themes, the launcher didn't. GAP NOTE: the instrument certifies the DUMP path; the run_app launcher is a separate wiring surface that can drift — worth a lightweight "launched app == dumped app" check later.

- [shell] APP SHELL now launchable (user-facing gap the per-screen instrument never covered): run_app.py gained (1) DISPLAY = 411dp→412 wide (rounded up to a multiple of 4: an odd width shears the screenshot via GL_PACK_ALIGNMENT — live app was fine, saved PNG sheared) × 915 portrait, matching the instrument's w411dp-h915dp ground truth (was Kivy's 800×600 landscape default → looked like a squat tablet). (2) Full-app mode: no-arg / `run_app.py App` composes AppNavigation (Scaffold + bottom NavigationBar + NavHost), seeds a completed user so it boots to Today with the nav bar (not onboarding). Single-screen mode (`run_app.py <Screen>`) unchanged. Both render clean portrait now. The bottom nav bar is FULLY FUNCTIONAL (see next line).
- [shell] SHELL IN THE AUTOMATED GATE (Sonnet slice): interact.py now sweeps AppNavigation's Scaffold slots (topBar/bottomBar/FAB — content slot excluded, that's the destination sweep) with ROUTE ASSERTIONS: a nav tap that runs but doesn't change currentRoute() is a FAILURE. New line: `INTERACT: 513/513 across 27 screens + shell(5 handlers)`, deterministic x2. Also found+fixed instrument hygiene: importing run_app leaked OVERLAYS_ENABLED=True into the destination sweep (513→515 drift); now reset after import, enabled only during the shell phase. Report: DevComms/shell_sweep.md.
- [assets] Emoji font vendored (Sonnet slice): render/assets/NotoColorEmoji.ttf + OFL/Apache license (Debian copyright provenance); path resolution already preferred vendored. Both fonts (icons + emoji) now ship with the repo — no OS package dependencies. Report: DevComms/emoji_vendoring.md.
- [shell] TOUCH DISPATCH GENERALIZED + NAV BAR M3 GEOMETRY (both user-found; the instruments' blind spot — interact.py fires handlers directly, so real-touch dispatch was never tested): (1) taps were bound only in the button factory (BUTTON_KINDS whitelist) — clickable Cards/Rows/ListItems/NavigationBarItems recorded onClick but were touch-dead; now _bind_click at the to_widget choke point gives ANY onClick-bearing node a real touch target (12px slop = scroll-safe; innermost-wins via touch.ud; routed through _fire). (2) NavigationBar had no spec geometry (wrap-by-default shrank it); now NAVBAR_H=80 + full width + EQUAL_DIVIDE_KINDS, icon-over-label centered. REAL-TAP gate (synthetic touches via EventLoop.post_dispatch_input, re-run by Fable): Progress→progress, You→settings, 140px drag→no fire. Gates: 377/377, 513/513. Report: DevComms/touch_dispatch_navbar.md; shots navfix_before/after.
- [shell] NAV BAR VERIFIED WORKING (corrected: an earlier note here wrongly called it inert): NavigationBar/NavigationBarItem ARE real _composable components (in compose.py _NAMES) and DO register onClick. Tapping each item routes correctly and the NavHost swaps content — verified programmatically (today→progress→paths→my_program→settings→today) and by screenshot (nav_progress_proof: Progress screen under the persistent top+bottom bars). The "only the FAB has a handler" reading was a PROBE ARTIFACT: AppNavigation's first compose() renders the loading-spinner branch (onboardingCompleted flow still None, ~15 nodes); the nav bar only exists after the tree SETTLES (fresh compose() per frame, as WflApp._remount does — settles to 42 nodes / 6 handlers). Lesson: settle headless probes with repeated compose() before reading the tree. run_app FIRE now matches a handler whose SUBTREE contains the label (was matching the text leaf, which has no onClick). FOLLOW-UP (not a defect): the shell (AppNavigation) isn't in interact.py's sweep — add it so nav-bar taps are part of the automated gate.
- [behavior] KOTLIN TEST SUITE PASSES IN PYTHON: 160/160 across all 11 app test classes (9 engines + PathDefinition + SampleProgramData) via new `run_kotlin_tests.py` — method counts match Kotlin @Test counts exactly, zero new transpiler fixes needed (rendering-round fixes carried the domain). Named limitation: oracle.py --all still aborts on an unrelated corpus test (java.util.TimeZone unshimmed). Report: DevComms/kotlin_tests_oracle.md.
- [behavior] Interaction oracle LIVE and CLEAN (`render/interact.py`): fires every recorded handler on every destination from a fresh seeded DB + fresh compose per handler. First sweep found 15 failures in 10 root-cause groups; fix wave 1 (delegated) closed all app-side groups with general fixes: (1) cross-file trailing-lambda slot binds to the callee's real last param via a build pre-pass registry; (2) new kstrext CharSequence-extension dispatcher (str gets Kotlin String.filter/etc.) + Char predicate mapping; (3) bare stdlib calls on extension `this` routed through receiver rewrite; (4) KtMap.maxByOrNull/minByOrNull/maxWithOrNull; (5) BIG ONE — remember/slot table was keyed by raw ordinal so conditional content shifted later slots onto foreign values; re-keyed by call site + occurrence index (Compose's positional memoization), hardening State app-wide. Instrument-side: recorder keeps the typed TextFieldValue in props; probe mirrors the value's type. NOW: 513 fired, 513 ok, 0 failures, 27 screens; kotlin tests 160/160; geometry 377/377 — all three gates re-verified together. Reports: DevComms/interaction_oracle.md, interact_fix_wave1.md. Report: DevComms/interaction_oracle.md.
- [transpiler] .kt line map DONE: transpiler tags each rendered statement's first line with its Kotlin line (opt-in `_srcmap`, build-only — fresh transpile output byte-identical); build extracts + strips into `<module>.py.linemap.json` sidecars (254/254); inspector deep-links `kt:<file>:<line>` with the Kotlin line text, file-level fallback on a miss. 5 hand spot-checks correct; all three gates re-verified (160/160, 513/513, 377/377). Report: DevComms/kt_linemap.md.
- [domain] Oracle breadth DONE (honestly cataloged): oracle.py --all now runs to completion (instance construction moved inside per-test try) — 11/12 subjects fully green (160 assertions, matches run_kotlin_tests.py); 12th is LayoutDumpTest, ERR by design (it needs the real Compose engine — no shim-for-show; TimeZone appears in zero main-source files). DAO layer covered by datalayer_oracle.py: 4 androidTest classes, 9 invariants (tx rollback, backup round-trip, 4 migrations) all green on real sqlite3. Uncovered-and-listed: repositories beyond those 4 have no app-authored tests to run. Report: DevComms/oracle_breadth.md.
- [behavior] ACCEPTANCE-DRIVE ROUND 1 (two user-found bugs, both fixed at the CAUSE): (1) Programs add-program overlay dead — NOT touch wiring: di.py viewmodel_of swallowed VM construction exceptions (seed user lacked trainingExperience → ProgramsViewModel threw → inert placeholder → all three dialog guards true → three stacked AlertDialogs). Fix: viewmodel_of FAILS LOUD (RuntimeError naming the VM, chained traceback) + run_app seed user completed per the real UserEntity schema/onboarding defaults. (2) Paths Start Path no-op — DB write landed but Dao._flow ran its SQL ONCE (static Flow, no Room invalidation): every Flow-backed screen stale after any write. Fix: real InvalidationTracker semantics in runtime/room.py — tables parsed from SQL, per-table subscriber registry, every write path invalidates (tx defers to outermost COMMIT, rollback discards), _flow/_flowOne/_relation re-run+re-emit; coroutines.combine made invalidation-aware (eager stateIn VM patterns stay correct). GATE EXTENDED for both classes: realtap_gate.py --case reactivity (real taps: picker→select→Start Path→Paths screen shows the enrolled path) + --case overlay (exactly ONE dialog, dismiss works); negative-tested (inverted assertion goes RED). All gates green after both fixes: fidelity 377/377 (compose ground truth reused — zero Kotlin diff; kivy side + diff fully re-run), interact 513/513 + shell 5/5, realtap GREEN (old 3 + new 2), kotlin tests 160/160, datalayer ALL GREEN. NOTE: run in the Cowork sandbox (45s/call, no gradle) — interact/fidelity executed CHUNKED per screen/handler-slice from a staged copy; re-run plain `fidelity.py` + `interact.py` on the host to reconfirm end-to-end. Reports: DevComms/overlay_path_diagnosis.md, room_flow_invalidation.md, di_failloud_seed.md, realtap_reactivity_gate.md.
- [behavior] ACCEPTANCE-DRIVE ROUND 2 (two user-found bugs → three general fixes, one instrument fix): (1) Path select gave no visual feedback — paint gap: CardDefaults/BorderStroke were inert autostubs discarding color/border args, and _paint_spec never read per-instance `colors=`/`border=`. Fix: value-retaining Defaults objects (Card/Button/FilterChip/AssistChip/TopAppBar — the grep-audited set the app passes explicitly), theme-resolved omitted args, kit honors recorded colors/border over the role table (selected path card: primaryContainer fill + 2dp primary border per PathSelectionSheet.kt:185-194). (2) Program enroll not reflected on Today — flatMapLatest evaluated its lambda ONCE (combine got the live fix last round, siblings didn't). Fix: generic _add_listener/_push on base Flow; flatMapLatest now truly switches (old inner unsubscribed); same one-shot defect fixed in map/mapNotNull/filter/onEach/collect/collectLatest/stateIn + room._LiveMixin.flatMapLatest; first/take/drop/toList correctly stay one-shot. (3) Live reactivity exposed latent KtMap.keys/.values (raw dict, no stdlib chaining) — now KtSet/KtList wrappers. (4) interact.py infer_value special-cased onValueChange only; now ANY on*Change/on*ChangeFinished mirrors its prop by naming convention (onQueryChange → query) — the 2 instrument failures cleared. GATES: kotlin tests 160/160, datalayer ALL GREEN, realtap (5 cases) GREEN, interact 542/542 + shell 5/5 (baseline grew 513→542: live flows render fuller trees). FIDELITY 403/406: the 3 out-of-band components on ProgramsScreen/ExercisesScreen are the STALE compose ground truth (dumps predate live flows), not geometry regressions — HOST ACTION: re-run the gradle LayoutDump + `fidelity.py` on the host to re-baseline (sandbox has no gradle). Reports: DevComms/selection_feedback_diagnosis.md, flatmaplatest_live.md, ktmap_fix_gate_reconciliation.md, card_selected_paint.md.
- [ui] DENSITY SCALING (user request — bigger window, everything scales): real Compose density semantics via ONE factor at kivy_kit's dp()/sp() choke point (`set_density()`, default 1.0 = exact identity); ~30 hardcoded px constants (navbar/topbar heights, icon/button sizes, radii, borders, shadows, tap slop) routed through it; fonts re-rasterize at scaled size (crisp, no texture stretch). run_app reads `WFL_SCALE` (e.g. =2 → 824x1830, width kept multiple-of-4 for the GL_PACK_ALIGNMENT shear rule). Instrument path untouched: inspect_layout never reads WFL_SCALE, density-1 dumps byte-identical (verified). realtap GREEN at 1x and 2x (touch coords scale). CORRECTED fidelity ledger: honest current number is 374/377 — the 3 misses are ProgramsScreen(2)/ExercisesScreen(1) against the STALE pre-live-flow compose ground truth (round 2's "403/406" was an agent misreport; re-verified by full chunked re-tally). HOST ACTION unchanged: re-run `fidelity.py` (gradle re-dump) to re-baseline. Report: DevComms/density_scaling.md.
- [walker] SLICE 1 LIVE — the decision-tree walker (user's instrument to replace the manual acceptance drive): render/walker.py walks the REAL app (run_app composition, real synthetic touches via post_dispatch_input — never direct handler calls), bounded BFS, checkpoint/--resume (survives hard kills), deterministic (byte-identical JSON x2). Format is engine-agnostic for the Kotlin recorder (slice 2): state = sha256(route + ordered {kind,text,interactive} tree summary, geometry excluded — that's fidelity's job); edge = {source, action{kind,label,handler_kind,ordinal}, dest, error}; dialogs/menus are states (`route#dialog:Kind`). Recovery = fresh reboot + tap-path replay per edge (~7-8s/edge, most deterministic). Three real walker bugs found+fixed in debug passes: (1) settle() mounted only on non-stable rounds — the stabilized tree was never mounted (orphan generation, every tap LookupError); (2) kivy import swallowed sys.argv (argparse silently got defaults) — realtap_gate's save-argv pattern; (3) _remount never pumped EventLoop.idle() so Kivy's deferred layout never ran — every widget at default (0,0,100x100), all taps landed at (50,50) on Today content and nav "never changed route" (app was fine; realtap_gate got layout passes free from its live Clock delays). Verified: 5/5 nav destinations reached, determinism x2, resume mid-edge. NEXT: slice 2 Kotlin recorder (Robolectric, same format — runs on host), slice 3 differ + gate line. Report: DevComms/walker_slice1.md.
- [walker] SLICE 2 EXECUTED GREEN + host loop + emulator dry build: (1) walk_service.py — host-side request/result loop through the repo (user starts one script; requests in DevComms/hostruns/requests, logs in results; no-shell exec, ~/Programming fence, command allowlist). Removed the user from the gradle loop — 24 round-trips this session ran through it. (2) WalkRecorderTest GREEN: Hilt test setup (@HiltAndroidTest/HiltTestApplication; test-only deps), real MainActivity mount with additive default-off EXTRA_SKIP_SETCONTENT, scenario.recreate() per replay, and the closer — per-mount Room executor + db teardown (the SAME ArchTaskExecutor-contention class the dump suite hit; leaked collectors kept pools alive, deeper replays starved the looper). kt_walk.json real: 30 states/51 edges/11 routes at steps=20. OPEN ANOMALIES for slice 3: steps=20 vs steps=60 recorded DIFFERENT trees (30/51 vs 22/53 — step-budget semantics or nondeterminism, unresolved); one text field accumulates "42" across BFS paths (walk-policy state pollution, 1 state/1 edge divergence between runs). (3) Emulator walk DRY BUILD (UNEXECUTED): CommandReceiver ported to current schema (debug-only manifest; dump_db deterministic; dump_semantics honestly unsupported — UI comes from uiautomator outside), render/emu_walker.py + vendored emuwalk_utils.py, line-level policy parity vs walker.py (2 drifts fixed, dialog-tagging gap flagged). (4) FIDELITY RE-BASELINED on host: 354/357 — ExercisesScreen 6/7 + ProgramsScreen 1/3 are REAL geometry misses vs fresh compose ground truth (totals moved 377→357 with livelier flows), undiagnosed. Reports: DevComms/walker_slice2_execution.md, emulator_walk_drybuild.md.
- [ui] Scaffold innerPadding + Modifier order: geometry gauge is at 377/377 with these as-is — no measured evidence of a defect remains; reopen only if a specimen/screen shows one.

Recently shipped:
- History renders a real session card, byte-identical text to compose — the instrument caught 4 CONTENT bugs: Kotlin printf-format (.format/String.format) transpiled to brace-format (silent template passthrough), TemporalAdjusters stubbed (week starts wrong), java date patterns' quoted literals mangled ('at' -> 'AMt'), and Int32 overflow inside timedelta (plusDays(6) == -1 day). All fixed in transpiler/runtime; 254/254 regenerated.
- 4 of 5 screens at/near-perfect: GymList 7/7, LogCardio 23/25, Exercises 7/7, Today 3/3; Settings 22/44. One session took the gauge 24/39 → 62/86 (5 screens) via: M3 slot order, class heights/insets, real line-height stacking (app Typography honored end-to-end), popups excluded from layout, harness renders inside the app theme, loader same-package shadowing (Kotlin visibility), real Font/FontFamily + variable-font weight instancing, off-viewport differ rule.
- Input fields render their slot subtrees: a node with text AND children is a field container (value as child label), not a leaf — one fix removed the 100px stepper block AND the "Notes (optional)" MISS. BasicTextField dropped from the 56dp rule (it's foundation, undecorated); empty Box collapses instead of Kivy's 100x100 default. Save Δ26.9→0.2.
- Layout-fidelity instrument: LayoutDumpTest (real Compose boxes, headless JVM) + inspect_layout JSON + layout_diff (%-of-display, tolerance band) → per-screen fidelity %, now a measured gauge (`fidelity.py`).
- Layout inspector (`layout_inspect/*.html`): per component — the code line that created it (file:line, .kt path) ‖ declared shape ‖ live computed box.
- Compose measure/place reconstructed on Kivy: wrap-by-default, Box z-stack, arrangement spacers, Scaffold slot framing + FAB float, fill-vs-scroll reconciliation, weight-on-parent-axis, top-pack columns (Kivy bottom-packs spare space — measured), M3 type scale + TopAppBar/icon-button spec geometry, `then`-chain splicing, Spacer(weight).
- Theme tokens live: real CompositionLocal — WflTheme.tokens.* resolve to real dp values (24 files move together).
